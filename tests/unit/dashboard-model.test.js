import test from "node:test";

test.todo("filterTickets keeps matching independent from uppercase and surrounding spaces");
test.todo("summarizeTickets returns zero counts for an empty ticket list");
