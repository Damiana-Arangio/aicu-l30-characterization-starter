{
  "name": "aicu-l30-characterization-starter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <27"
  },
  "packageManager": "pnpm@11.5.1",
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "reset": "node scripts/reset-data.js",
    "check": "node --check server/index.js && node --check server/app.js && node --check server/ticket-repository.js && node --check server/ticket-service.js && node --check server/legacy-summary-provider.js && node --check server/ticket-history-repository.js && node --check server/ticket-history-service.js && node --check src/dashboard-model.js && node --check src/main.js && node --check scripts/reset-data.js && node --check scripts/measure-dashboard.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "test:watch": "node --test --watch \"tests/**/*.test.js\"",
    "measure:dashboard": "node scripts/measure-dashboard.js",
    "verify": "pnpm check && pnpm test"
  }
}
